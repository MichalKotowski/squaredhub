{
    "name": "Squared Hub",
    "version": "1.0.0",
    "main": "",
	"engines": {
		"node": "18.3.0",
		"npm": "8.11.0"
	},
    "scripts": {
        "start": "npm start --prefix server",
        "install-client": "cd client && npm install && npm run build && cd ..",
        "install-server": "cd server && npm install && npm run start",
        "heroku-postbuild": "npm run install-client && npm run install-server"
    }
}